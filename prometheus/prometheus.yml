global:
  scrape_interval: 5s # Scrape quickly for testing

scrape_configs:
  - job_name: "my-fastapi-app"
    static_configs:
      - targets: ["app:8000"] # Points to the app container inside Docker
